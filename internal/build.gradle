plugins {
    id 'java-library'
}
def le_cbchain = '1.0.0'

dependencies {
    compile group: 'org.liquidengine', name: 'cbchain', version: le_cbchain, changing: true
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

ext.moduleName = 'org.liquidengine.legui.internal'