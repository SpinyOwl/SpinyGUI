plugins {
    id 'java-library'
}

dependencies {
    api project(':core')

    api project(':core.backend')
    api project(':core.backend.lwjgl.nanovg')

    api group: 'com.spinyowl', name: 'cbchain', version: '1.0.0';
}